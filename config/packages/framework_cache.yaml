framework:
    cache:
        pools:

            grinway_telegram.cache_pool:
                adapters:
                - 'cache.adapter.filesystem'
                default_lifetime: '%env(string:default:grinway_telegram.cache.lifetime:)%'
                tags: true
